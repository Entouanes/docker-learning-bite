{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Clean Docker Images","text":"<p>A short self-guided tutorial. You'll go from zero to writing lean, secure, production-ready Docker images \u2014 in under an hour.</p>"},{"location":"#what-youll-learn","title":"What you'll learn","text":"<ul> <li> <p> How Docker works</p> <p>Images, containers, and the layer system \u2014 the mental model that makes everything else click.</p> <p> How Docker Works</p> </li> <li> <p> Writing Dockerfiles</p> <p>Every key instruction explained clearly, with common mistakes to avoid.</p> <p> Dockerfile Guide</p> </li> <li> <p> Best practices</p> <p>Layer ordering, cleanup, non-root users, and a look at multi-stage builds.</p> <p> Best Practices</p> </li> <li> <p> Hands-on lab</p> <p>Build a real image, measure its size, fix it, then use a multi-stage build to shrink it further.</p> <p> Start the Lab</p> </li> </ul>"},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker installed and running (<code>docker --version</code>)</li> <li>Basic comfort with a terminal and text editor</li> <li>No prior Docker knowledge required</li> </ul>"},{"location":"#how-this-tutorial-is-structured","title":"How this tutorial is structured","text":"Section What it covers Time How Docker Works Images, layers, containers ~5 min Dockerfile Guide Key instructions ~5 min Best Practices Techniques for clean images ~5 min Lab Part 1 Build, measure, fix ~15 min Lab Part 2 Multi-stage builds ~10 min <p>Total: ~40 minutes. Read the three concept pages first, then do the lab.</p>"},{"location":"concepts/best-practices/","title":"Best Practices","text":"<p>Three techniques separate a bloated, fragile image from a lean, production-ready one.</p>"},{"location":"concepts/best-practices/#1-order-layers-slow-changing-fast-changing","title":"1. Order layers: slow-changing \u2192 fast-changing","text":"<p>Docker caches each layer. Once a layer changes, all layers below it are rebuilt. This means the order of your instructions has a huge impact on build speed.</p> <p>The rule: Put things that change rarely near the top. Put things that change often near the bottom.</p> <pre><code>Most stable                                        Least stable\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba\nFROM base  \u2192  OS packages  \u2192  dependency manifest  \u2192  install deps  \u2192  source code\n</code></pre> \u274c Slow \u2014 deps reinstall on every code change\u2705 Fast \u2014 deps only reinstall when requirements.txt changes <pre><code>FROM python:3.12-slim\nWORKDIR /app\nCOPY . .                          # copies ALL source code\nRUN pip install -r requirements.txt  # reinstalls everything if ANY file changed\n</code></pre> <pre><code>FROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .           # copy only the manifest\nRUN pip install -r requirements.txt  # cached until requirements.txt changes\nCOPY . .                          # copy source code last\n</code></pre> <p>Why this matters</p> <p>On a real project, <code>pip install</code> or <code>npm install</code> can take 2\u20135 minutes. With the correct ordering, that step runs from cache on every routine code change \u2014 making rebuilds nearly instant.</p>"},{"location":"concepts/best-practices/#2-choose-a-minimal-base-image","title":"2. Choose a minimal base image","text":"<p>The base image is the foundation everything else builds on. Smaller base = smaller final image = faster pulls, fewer CVEs.</p> Base image Approximate size Best for <code>scratch</code> 0 MB Statically compiled Go or Rust binaries Google Distroless ~2\u201320 MB Production Java, Python, Node.js runtimes <code>alpine:3.20</code> ~7 MB General-purpose minimal images <code>python:3.12-slim</code> ~130 MB Most Python apps \u2705 <code>python:3.12</code> ~1 GB Only when you need the full Debian toolchain <p>Alpine caveats</p> <p>Alpine uses <code>musl libc</code> instead of <code>glibc</code>. Most Python packages work fine, but some C extensions (numpy, pandas, certain database drivers) behave differently or require recompilation. Test before committing to Alpine.</p>"},{"location":"concepts/best-practices/#3-use-multi-stage-builds","title":"3. Use multi-stage builds","text":"<p>Multi-stage builds let you use a heavy \"builder\" image for compilation and only copy the result into a lightweight \"runtime\" image.</p> <pre><code># Stage 1: build\nFROM python:3.12 AS builder          # full image \u2014 has pip, compilers\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --prefix=/install -r requirements.txt\n\n# Stage 2: runtime\nFROM python:3.12-slim                # slim image \u2014 just what we need to run\nWORKDIR /app\nCOPY --from=builder /install /usr/local   # copy only the installed packages\nCOPY src/ .\nCMD [\"python\", \"app.py\"]\n</code></pre> <p>The <code>builder</code> stage is completely discarded \u2014 it never makes it into the final image. Only the <code>COPY --from=builder</code> lines survive.</p> <p>Real-world size reductions</p> Language Without multi-stage With multi-stage Java ~880 MB ~428 MB Go ~800 MB ~5\u201320 MB (into <code>scratch</code>) Node.js ~900 MB ~150 MB"},{"location":"concepts/best-practices/#4-use-dockerignore","title":"4. Use <code>.dockerignore</code>","text":"<p>When you run <code>docker build .</code>, Docker sends your entire current directory to the daemon before the build starts. A <code>.dockerignore</code> file tells Docker what to exclude \u2014 similar to <code>.gitignore</code>.</p> <pre><code># .dockerignore\n.git/\n.venv/\n__pycache__/\n*.pyc\nnode_modules/\n.env\n*.log\n</code></pre> <p>Two reasons to always use <code>.dockerignore</code></p> <ol> <li>Speed \u2014 sending <code>node_modules/</code> or <code>.venv/</code> can add seconds or minutes before a single layer is built.</li> <li>Security \u2014 without it, <code>.env</code> files, SSH keys, or other secrets in your directory can accidentally end up in the image.</li> </ol>"},{"location":"concepts/best-practices/#5-run-as-non-root","title":"5. Run as non-root","text":"<p>Never ship a production image where your app runs as root. Create a dedicated system user and switch to it before the final <code>CMD</code> or <code>ENTRYPOINT</code>.</p> <pre><code>RUN useradd --system --no-create-home appuser\nUSER appuser\n</code></pre> <p>See Dockerfile Guide \u2192 USER for details.</p>"},{"location":"concepts/best-practices/#quick-reference","title":"Quick reference","text":"Practice Why it matters Order: stable \u2192 volatile Keeps expensive layers cached Minimal base image Fewer CVEs, faster pulls Multi-stage builds Discard build tools from the final image <code>.dockerignore</code> Faster builds, no accidental secrets Non-root <code>USER</code> Limits blast radius of a compromise Pin image tags Reproducible, no surprise changes Cleanup in same <code>RUN</code> Phantom deletions don't reduce size <p>You're ready for the lab</p> <p>You now have the mental model and the techniques. Time to put them into practice.</p> <p> Start the Lab</p>"},{"location":"concepts/dockerfile-guide/","title":"Dockerfile Guide","text":"<p>A Dockerfile is a plain-text list of instructions that Docker executes top to bottom to build an image. Here are the instructions you'll use most \u2014 what they do, and what to watch out for.</p>"},{"location":"concepts/dockerfile-guide/#from-choose-your-base","title":"<code>FROM</code> \u2014 Choose your base","text":"<p>Every Dockerfile starts with <code>FROM</code>. It sets the base image all subsequent layers build on.</p> <pre><code>FROM python:3.12-slim\n</code></pre> <p>Pick the right variant</p> <p>Most official images come in several flavours. Start with <code>slim</code> or <code>alpine</code> \u2014 they're much smaller than the default.</p> Tag Approximate size Use when <code>python:3.12</code> ~1 GB You need full Debian toolchain <code>python:3.12-slim</code> ~130 MB Most Python apps \u2705 <code>python:3.12-alpine</code> ~55 MB Size is critical; aware of musl differences <p>Never use <code>latest</code></p> <p><code>FROM python:latest</code> is a moving target \u2014 your image will silently change whenever the publisher updates the tag. Always pin to a specific version: <code>FROM python:3.12.3-slim</code>.</p>"},{"location":"concepts/dockerfile-guide/#workdir-set-the-working-directory","title":"<code>WORKDIR</code> \u2014 Set the working directory","text":"<p>Sets the current directory for all following instructions. Creates the directory if it doesn't exist.</p> <pre><code>WORKDIR /app\n</code></pre> <p>Tip</p> <p>Always use <code>WORKDIR</code> instead of <code>RUN cd /path &amp;&amp; ...</code>. A <code>cd</code> in <code>RUN</code> only lasts for that one instruction. <code>WORKDIR</code> persists.</p>"},{"location":"concepts/dockerfile-guide/#copy-vs-add-bring-files-into-the-image","title":"<code>COPY</code> vs <code>ADD</code> \u2014 Bring files into the image","text":"<p>Use <code>COPY</code> by default. It does exactly one thing: copies files from your build context into the image.</p> <pre><code>COPY requirements.txt .\nCOPY src/ ./src/\n</code></pre> <p>Use <code>ADD</code> only when you specifically need its extras:</p> Need Use Copy local files <code>COPY</code> Copy from another build stage <code>COPY --from=builder</code> Auto-extract a local <code>.tar.gz</code> <code>ADD archive.tar.gz /dest/</code> Download from a URL <code>ADD https://example.com/file.tar.gz /dest/</code> <p>Warning</p> <p><code>ADD</code> silently extracts tar archives. That can be surprising. Prefer <code>COPY</code> unless you need the extras.</p>"},{"location":"concepts/dockerfile-guide/#run-execute-commands-at-build-time","title":"<code>RUN</code> \u2014 Execute commands at build time","text":"<p>Runs a shell command and commits the result as a new layer.</p> <pre><code>RUN apt-get update \\\n    &amp;&amp; apt-get install -y --no-install-recommends curl \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n</code></pre> <p>The <code>&amp;&amp;</code> chaining is intentional. Remember the phantom deletion rule: cleanup must happen in the same <code>RUN</code>.</p> <p>Use <code>--no-install-recommends</code></p> <p>With apt, this skips suggested packages and significantly reduces layer size.</p>"},{"location":"concepts/dockerfile-guide/#env-vs-arg-variables","title":"<code>ENV</code> vs <code>ARG</code> \u2014 Variables","text":"<pre><code>ARG APP_VERSION=1.0       # Build-time only \u2014 not in the running container\nENV APP_ENV=production    # Persists into the running container\n</code></pre> Feature <code>ENV</code> <code>ARG</code> Available during build \u2705 \u2705 Available at runtime \u2705 \u274c Stored in image metadata \u2705 \u274c Safe for secrets \u274c \u274c <p>Never put secrets in <code>ENV</code> or <code>ARG</code></p> <p><code>ENV</code> values appear in <code>docker inspect</code>. <code>ARG</code> values appear in <code>docker history</code>. Both are readable by anyone with access to the image. Use <code>RUN --mount=type=secret</code> (BuildKit) for build-time secrets, or secrets managers for runtime.</p>"},{"location":"concepts/dockerfile-guide/#expose-document-ports-documentation-only","title":"<code>EXPOSE</code> \u2014 Document ports (documentation only)","text":"<pre><code>EXPOSE 8080\n</code></pre> <p><code>EXPOSE</code> does not open any ports. It's documentation \u2014 it tells users and tooling what port the app uses. To actually publish a port, use <code>-p 8080:8080</code> when running the container.</p>"},{"location":"concepts/dockerfile-guide/#cmd-vs-entrypoint-what-runs-when-the-container-starts","title":"<code>CMD</code> vs <code>ENTRYPOINT</code> \u2014 What runs when the container starts","text":"<p>These two are the most confusing pair in Docker. Here's the simple version:</p> Instruction Purpose Can be overridden? <code>CMD</code> Default command (easily replaceable) Yes \u2014 by passing a command to <code>docker run</code> <code>ENTRYPOINT</code> Fixed executable (the container is this program) Only with <code>--entrypoint</code> flag <p>Common patterns:</p> <pre><code># Pattern 1: Simple app \u2014 just use CMD\nCMD [\"python\", \"app.py\"]\n\n# Pattern 2: Fixed service \u2014 use ENTRYPOINT for the binary, CMD for default flags\nENTRYPOINT [\"gunicorn\"]\nCMD [\"--bind\", \"0.0.0.0:8080\", \"app:app\"]\n\n# Pattern 3: CLI tool\nENTRYPOINT [\"mytool\"]\n# Users run: docker run myimage --help\n</code></pre> <p>Always use exec form for ENTRYPOINT</p> <p>Shell form (<code>ENTRYPOINT python app.py</code>) wraps your command in <code>/bin/sh -c</code>, making the shell PID 1. Your app never receives <code>SIGTERM</code> from <code>docker stop</code>, so graceful shutdown is impossible.</p> <pre><code># \u274c Shell form \u2014 shell becomes PID 1\nENTRYPOINT python app.py\n\n# \u2705 Exec form \u2014 your app is PID 1\nENTRYPOINT [\"python\", \"app.py\"]\n</code></pre>"},{"location":"concepts/dockerfile-guide/#user-run-as-non-root","title":"<code>USER</code> \u2014 Run as non-root","text":"<p>By default, container processes run as root. In production, always switch to a non-privileged user.</p> <pre><code>RUN useradd --system --no-create-home appuser\nUSER appuser\n</code></pre> <p>Why non-root matters</p> <p>If an attacker exploits a vulnerability in your app, running as root inside the container gives them much more power \u2014 especially in container escape scenarios. A non-root user limits the blast radius.</p> <p>Key takeaways</p> <ul> <li>Pin your <code>FROM</code> tag \u2014 never use <code>latest</code></li> <li>Use <code>WORKDIR</code>, not <code>RUN cd</code></li> <li>Prefer <code>COPY</code> over <code>ADD</code> unless you need the extras</li> <li>Chain install+cleanup in the same <code>RUN</code></li> <li>Use exec form for <code>ENTRYPOINT</code></li> <li>Never put secrets in <code>ENV</code> or <code>ARG</code></li> <li>Always switch to a non-root <code>USER</code> in production</li> </ul>"},{"location":"concepts/how-docker-works/","title":"How Docker Works","text":"<p>Docker solves the \"it works on my machine\" problem by packaging your app and everything it needs into a portable, isolated unit called a container.</p>"},{"location":"concepts/how-docker-works/#containers-vs-virtual-machines","title":"Containers vs. Virtual Machines","text":"<p>The most important distinction: containers are not mini VMs.</p> Virtual Machine Docker Container Isolates Hardware (full guest OS) Process (shared host kernel) Boot time Minutes Milliseconds Size Gigabytes Megabytes Density ~10s per host 100s per host <p>A VM virtualizes hardware and runs a complete OS. A container virtualizes the OS userspace \u2014 it's just an isolated process with its own filesystem view. Multiple containers share the same Linux kernel on the host.</p> <p>Real-world setup</p> <p>In the cloud, your VMs host many containers. Docker runs on top of the VM \u2014 not instead of it.</p>"},{"location":"concepts/how-docker-works/#images-and-containers","title":"Images and Containers","text":"<p>Think of it like a class and its instances:</p> Concept Analogy Description Dockerfile Recipe Instructions for building the image Image Mold / class Immutable, read-only blueprint Container Cookie / instance A running image with a writable layer on top <p>One image \u2192 many containers. Each container gets its own thin writable layer; the underlying image is shared and never modified.</p> <p>Containers are ephemeral</p> <p>Any data written inside a container is lost when the container is removed. Use volumes to persist data.</p>"},{"location":"concepts/how-docker-works/#the-layer-system","title":"The Layer System","text":"<p>A Docker image is not a single flat file \u2014 it's a stack of immutable filesystem layers, each representing the changes made by one build step.</p> <pre><code>Layer 4 (top) \u2190 COPY app/ /app                  [your source code]\nLayer 3       \u2190 RUN pip install -r requirements  [your dependencies]\nLayer 2       \u2190 COPY requirements.txt /app/      [dependency manifest]\nLayer 1       \u2190 RUN apt-get install python3      [Python runtime]\nLayer 0 (base)\u2190 FROM ubuntu:22.04               [base OS]\n</code></pre> <p>Each layer stores only the diff from the previous one \u2014 additions, deletions, modifications. Layers are identified by a SHA256 hash and shared across images: if two images share a base, they share those layers on disk.</p>"},{"location":"concepts/how-docker-works/#which-instructions-create-layers","title":"Which instructions create layers?","text":"<p>Only three Dockerfile instructions create new filesystem layers. Everything else is just metadata.</p> Creates a layer Metadata only <code>RUN</code>, <code>COPY</code>, <code>ADD</code> <code>CMD</code>, <code>ENTRYPOINT</code>, <code>ENV</code>, <code>ARG</code>, <code>EXPOSE</code>, <code>LABEL</code>, <code>USER</code>, <code>WORKDIR</code>, <code>VOLUME</code>, <code>HEALTHCHECK</code>"},{"location":"concepts/how-docker-works/#the-phantom-deletion-problem","title":"The Phantom Deletion Problem","text":"<p>This is the most important thing to understand about layers \u2014 and the most common mistake.</p> <p>Deleting a file in a later layer does NOT reduce image size</p> <p>The original bytes still exist in the earlier layer. Docker just adds a \"whiteout marker\" saying the file is hidden \u2014 but it's still transferred when you push or pull the image.</p> <pre><code># \u274c WRONG \u2014 the downloaded file is still in Layer 1\nRUN wget https://example.com/big-file.tar.gz\nRUN rm big-file.tar.gz\n\n# \u2705 CORRECT \u2014 cleanup in the same RUN; only the diff is committed\nRUN wget https://example.com/big-file.tar.gz \\\n    &amp;&amp; tar -xzf big-file.tar.gz \\\n    &amp;&amp; rm big-file.tar.gz\n</code></pre>"},{"location":"concepts/how-docker-works/#what-happens-when-you-run-docker-run","title":"What happens when you run <code>docker run</code>","text":"<ol> <li>Docker checks if the image exists locally; if not, pulls it from the registry.</li> <li>A new container is created with a thin writable layer on top of the image.</li> <li>A network interface is configured.</li> <li>The specified process starts inside the isolated environment.</li> <li>When the process exits, the container stops (but is not deleted until you <code>docker rm</code>).</li> </ol> <p>Key takeaways</p> <ul> <li>Containers share the host kernel \u2014 they're lightweight isolated processes</li> <li>Images are stacks of immutable layers; only <code>RUN</code>, <code>COPY</code>, <code>ADD</code> create layers</li> <li>Cleanup must happen in the same <code>RUN</code> instruction as the install</li> <li>One image can run as many containers; each gets its own writable layer</li> </ul>"},{"location":"lab/","title":"Lab Overview","text":"<p>In this lab you'll take a simple Python web app from a bloated, fragile image to a lean, production-ready one \u2014 measuring the difference at each step so you can see the impact of what you've learned.</p>"},{"location":"lab/#what-youll-build","title":"What you'll build","text":"<p>A minimal Python Flask app. The app itself is trivial \u2014 the point is the image, not the application.</p> <pre><code>lab/\n\u251c\u2500\u2500 app.py\n\u251c\u2500\u2500 requirements.txt\n\u2514\u2500\u2500 Dockerfile          \u2190 you'll evolve this through both parts\n</code></pre>"},{"location":"lab/#learning-outcomes","title":"Learning outcomes","text":"<p>By the end of the lab you will have:</p> <ul> <li> Built an image and measured its size with <code>docker images</code></li> <li> Identified wasted layers and fixed them</li> <li> Optimised layer ordering for fast rebuilds</li> <li> Converted a single-stage Dockerfile to a multi-stage build</li> <li> Seen a real before/after size comparison</li> </ul>"},{"location":"lab/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker installed and running</li> <li>A terminal and text editor</li> <li>Read the three concept pages (takes ~15 min)</li> </ul>"},{"location":"lab/#setup","title":"Setup","text":"<p>Create a working directory for the lab:</p> <pre><code>mkdir docker-lab &amp;&amp; cd docker-lab\n</code></pre> <p>Create the app files:</p> app.pyrequirements.txt <pre><code>from flask import Flask\n\napp = Flask(__name__)\n\n@app.route(\"/\")\ndef hello():\n    return \"Hello from Docker!\"\n\nif __name__ == \"__main__\":\n    app.run(host=\"0.0.0.0\", port=8080)\n</code></pre> <pre><code>flask==3.0.3\ngunicorn==22.0.0\n</code></pre>"},{"location":"lab/#lab-parts","title":"Lab parts","text":"Part Topic Time Part 1 \u00b7 Layer Fundamentals Build a bloated image, identify problems, fix them ~15 min Part 2 \u00b7 Multi-Stage Builds Use a builder stage, shrink the image further ~10 min <p> Start Part 1</p>"},{"location":"lab/part1/","title":"Part 1 \u00b7 Layer Fundamentals","text":"<p>In this part, you'll build a deliberately bad image, see why it's bad, and fix it step by step.</p>"},{"location":"lab/part1/#step-1-build-the-bloated-image","title":"Step 1: Build the bloated image","text":"<p>Create this <code>Dockerfile</code> in your <code>docker-lab/</code> directory:</p> Dockerfile (v1 \u2014 bloated)<pre><code>FROM python:3.12\n\nWORKDIR /app\n\n# Copy everything first (BAD order)\nCOPY . .\n\n# Install dependencies, download a big file, then try to delete it\nRUN pip install -r requirements.txt\nRUN apt-get update &amp;&amp; apt-get install -y curl\nRUN curl -o bigfile.bin https://releases.ubuntu.com/24.04/ubuntu-24.04.2-desktop-amd64.iso || \\\n    dd if=/dev/zero of=bigfile.bin bs=1M count=50\nRUN rm bigfile.bin\n\nEXPOSE 8080\nCMD [\"python\", \"app.py\"]\n</code></pre> <p>Note</p> <p>The <code>dd</code> command creates a 50 MB dummy file if the download fails \u2014 simulating a large build artifact. The point is to see that deleting it in a later layer does nothing.</p> <p>Build it:</p> <pre><code>docker build -t myapp:v1 .\n</code></pre> <p>Check the size:</p> <pre><code>docker images myapp\n</code></pre> <p>Note the size. It will be much larger than expected.</p>"},{"location":"lab/part1/#step-2-inspect-the-layers","title":"Step 2: Inspect the layers","text":"<pre><code>docker history myapp:v1\n</code></pre> <p>You'll see each layer and its size. Notice:</p> <ul> <li>The <code>RUN rm bigfile.bin</code> layer is tiny \u2014 but the layer that created the file is still large</li> <li>The large full <code>python:3.12</code> base (~1 GB) is the starting point</li> </ul>"},{"location":"lab/part1/#step-3-fix-problem-1-layer-ordering","title":"Step 3: Fix problem 1 \u2014 Layer ordering","text":"<p>Every time you change any file in your project, <code>COPY . .</code> at the top invalidates the cache, forcing <code>pip install</code> to run again. That's slow.</p> <p>Fix: copy the dependency manifest first, install, then copy the source.</p> Dockerfile (v2 \u2014 correct ordering)<pre><code>FROM python:3.12-slim              # (1) also switched to slim\n\nWORKDIR /app\n\nCOPY requirements.txt .            # (2) manifest only \u2014 stable\nRUN pip install -r requirements.txt  # cached until requirements.txt changes\nCOPY . .                           # (3) source code \u2014 changes often\n\nEXPOSE 8080\nCMD [\"python\", \"app.py\"]\n</code></pre> <p>Build it:</p> <pre><code>docker build -t myapp:v2 .\n</code></pre> <p>Now make a tiny change to <code>app.py</code> (e.g., change the greeting text) and rebuild:</p> <pre><code>docker build -t myapp:v2 .\n</code></pre> <p>Watch the output \u2014 <code>pip install</code> uses the cache. Rebuild is near-instant.</p>"},{"location":"lab/part1/#step-4-fix-problem-2-phantom-deletion","title":"Step 4: Fix problem 2 \u2014 Phantom deletion","text":"<p>The <code>rm bigfile.bin</code> in v1 didn't help because the file was created in an earlier layer. Cleanup must happen in the same <code>RUN</code> instruction.</p> Dockerfile (v3 \u2014 cleanup in same RUN)<pre><code>FROM python:3.12-slim\n\nWORKDIR /app\n\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt   # (1) --no-cache-dir avoids pip's own cache\n\n# Simulate a build artifact \u2014 create AND delete in the same RUN\nRUN apt-get update \\\n    &amp;&amp; apt-get install -y --no-install-recommends curl \\\n    &amp;&amp; dd if=/dev/zero of=bigfile.bin bs=1M count=50 \\\n    &amp;&amp; rm bigfile.bin \\                              # (2) delete in same RUN\n    &amp;&amp; rm -rf /var/lib/apt/lists/*                  # (3) clean apt cache too\n\nCOPY . .\n\nEXPOSE 8080\nCMD [\"python\", \"app.py\"]\n</code></pre> <p>Build and compare sizes:</p> <pre><code>docker build -t myapp:v3 .\ndocker images myapp\n</code></pre>"},{"location":"lab/part1/#step-5-before-after-comparison","title":"Step 5: Before / After comparison","text":"<p>Run this to see all three versions side by side:</p> <pre><code>docker images myapp --format \"table {{.Tag}}\\t{{.Size}}\"\n</code></pre> <p>You should see something like:</p> <pre><code>TAG    SIZE\nv3     ~200 MB\nv2     ~190 MB\nv1     ~1.3 GB\n</code></pre> <p>What you fixed</p> <ul> <li>Switched from <code>python:3.12</code> (~1 GB) to <code>python:3.12-slim</code> (~130 MB) \u2014 the single biggest win</li> <li>Fixed layer ordering \u2014 <code>pip install</code> now runs from cache on routine changes</li> <li>Fixed phantom deletion \u2014 cleanup happens in the same <code>RUN</code>, so deleted files don't bloat the image</li> </ul>"},{"location":"lab/part1/#checkpoint","title":"Checkpoint","text":"<p>Before moving on, make sure you understand:</p> <ul> <li> Why <code>RUN rm</code> in a separate step doesn't reduce image size</li> <li> Why <code>COPY requirements.txt</code> before <code>COPY . .</code> speeds up rebuilds</li> <li> Why switching from <code>python:3.12</code> to <code>python:3.12-slim</code> is the biggest single win</li> </ul> <p> Continue to Part 2</p>"},{"location":"lab/part2/","title":"Part 2 \u00b7 Multi-Stage Builds","text":"<p>In Part 1 you reduced the image size by fixing layer order and cleanup. In this part, you'll use a multi-stage build to go further \u2014 separating the build environment from the runtime image entirely.</p>"},{"location":"lab/part2/#the-idea","title":"The idea","text":"<p>The build tools you need to compile or package your app (compilers, pip, build headers) don't need to be in the final image. With multi-stage builds, you use a heavy builder image and copy only the output into a lightweight runtime image.</p> <pre><code>Stage 1 (builder) \u2500\u2500install deps\u2500\u2500\u25ba /install/\n                                         \u2502\n                                    COPY --from=builder\n                                         \u2502\nStage 2 (runtime) \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 /usr/local/\n</code></pre> <p>The builder stage is completely discarded \u2014 not tagged, not pushed, not visible in <code>docker images</code>.</p>"},{"location":"lab/part2/#step-1-convert-to-a-multi-stage-dockerfile","title":"Step 1: Convert to a multi-stage Dockerfile","text":"<p>Replace your <code>Dockerfile</code> with this:</p> Dockerfile (v4 \u2014 multi-stage)<pre><code># \u2500\u2500 Stage 1: builder \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nFROM python:3.12 AS builder           # (1) full image \u2014 has pip and build tools\n\nWORKDIR /app\n\nCOPY requirements.txt .\nRUN pip install --prefix=/install --no-cache-dir -r requirements.txt  # (2) install into /install\n\n# \u2500\u2500 Stage 2: runtime \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nFROM python:3.12-slim                 # (3) lightweight runtime image\n\nWORKDIR /app\n\nCOPY --from=builder /install /usr/local   # (4) only the installed packages \u2014 no pip, no build tools\nCOPY . .\n\nRUN useradd --system --no-create-home appuser  # (5) non-root user\nUSER appuser\n\nEXPOSE 8080\nCMD [\"gunicorn\", \"--bind\", \"0.0.0.0:8080\", \"app:app\"]\n</code></pre> <p>Annotations:</p> <ol> <li>The <code>AS builder</code> alias names this stage so we can reference it with <code>COPY --from=builder</code></li> <li><code>--prefix=/install</code> installs packages into <code>/install</code> instead of the system prefix \u2014 easier to copy</li> <li>The final stage starts fresh from the slim base; it has no memory of the builder stage</li> <li><code>COPY --from=builder</code> is the only bridge between stages</li> <li>We also add a non-root user \u2014 a production best practice</li> </ol>"},{"location":"lab/part2/#step-2-build-and-compare","title":"Step 2: Build and compare","text":"<pre><code>docker build -t myapp:v4 .\ndocker images myapp --format \"table {{.Tag}}\\t{{.Size}}\"\n</code></pre> <p>Expected output (approximate):</p> <pre><code>TAG    SIZE\nv4     ~160 MB\nv3     ~200 MB\nv2     ~190 MB\nv1     ~1.3 GB\n</code></pre> <p>What changed in v4?</p> <p>The final image contains only the installed Python packages and your source code. The full <code>python:3.12</code> image (with pip, build headers, and all of Debian's toolchain) was used to install dependencies \u2014 then discarded. It never made it into v4.</p>"},{"location":"lab/part2/#step-3-verify-it-works","title":"Step 3: Verify it works","text":"<p>Run the container to make sure everything actually works:</p> <pre><code>docker run -p 8080:8080 myapp:v4\n</code></pre> <p>Open http://localhost:8080 \u2014 you should see <code>Hello from Docker!</code>.</p> <p>Stop the container with Ctrl+C.</p>"},{"location":"lab/part2/#step-4-inspect-whats-inside","title":"Step 4: Inspect what's inside","text":"<p>Notice that build tools are gone from the final image:</p> <pre><code># Start a shell in the container\ndocker run --rm -it myapp:v4 sh\n\n# Inside the container \u2014 try these:\npip --version          # should fail \u2014 pip is not installed\npython --version       # should work \u2014 Python is there\ngunicorn --version     # should work \u2014 gunicorn was installed via COPY --from\nexit\n</code></pre>"},{"location":"lab/part2/#final-comparison","title":"Final comparison","text":"Version Change Size v1 Bloated \u2014 full base, phantom deletion ~1.3 GB v2 Fixed layer ordering ~190 MB v3 Fixed cleanup + slim base ~200 MB v4 Multi-stage + non-root ~160 MB <p>The biggest wins were: 1. Switching to <code>python:3.12-slim</code> \u2014 dropped from 1 GB+ to ~190 MB 2. Multi-stage build \u2014 removed pip and build tooling from the final image</p> <p>Congratulations \u2014 you're done!</p> <p>You've learned and applied every key technique for writing clean Docker images:</p> <ul> <li> The layer mental model and phantom deletion</li> <li> Optimised layer ordering for fast incremental builds</li> <li> Choosing a minimal base image</li> <li> Multi-stage builds to discard build tools</li> <li> Running as a non-root user</li> </ul> <p>Next steps: Try applying these techniques to a real project. Start with base image selection and layer ordering \u2014 those two changes alone often reduce image size by 80%.</p>"}]}