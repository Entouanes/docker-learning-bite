[project]
name = "docker-learning-bite"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-git-revision-date-localized-plugin>=1.5.1",
    "mkdocs-material[imaging]>=9.5.0",
]
